# Top-K Problem

Find the top K largest numbers from an array of 1 million elements.

Optimize for speed, memory usage, and code simplicity.

## Try it

```bash
cp -r examples/topk /tmp/topk-test
cd /tmp/topk-test
git init
sindri init
claude "/sindri loop"
```
